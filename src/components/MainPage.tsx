/* import { useSelector } from 'react-redux';
 */
function MainPage() {
  /*   const { uncontrolledFormData, hookFormData } = useSelector((state) => state.form);
   */
  return (
    <div>
      <h1>React Forms</h1>
      <p>Choose a form to try from the navigation bar above.</p>
    </div>
  );
}

export default MainPage;
